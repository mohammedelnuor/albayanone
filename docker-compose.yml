version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_SITE_URL=https://al-bayan.com
      - NEXT_PUBLIC_DEFAULT_LOCALE=en
    restart: always